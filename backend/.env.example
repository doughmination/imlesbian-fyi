# --- Server ---
PORT=4070
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:3000
FRONTEND_DASHBOARD_URL=http://localhost:3000/dashboard

# --- Database ---
DATABASE_URL=postgres://imlesbian:changeme@localhost:5432/imlesbian_fyi

# --- Sessions ---
# Generate with: openssl rand -base64 48
SESSION_SECRET=changeme_generate_a_real_32plus_char_secret

# --- Discord OAuth ---
# Create an application at https://discord.com/developers/applications
# Redirect URI must match EXACTLY what's registered there.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=http://localhost:4070/auth/discord/callback
